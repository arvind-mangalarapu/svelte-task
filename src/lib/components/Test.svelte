<!-- <script lang="ts">
	import Button from './Button.svelte'; // Adjust path as necessary
	import { showPopup } from '../../stores/showPopup.js'; // Import the store

	function togglePopup() {
		// Toggle the visibility of the popup
		showPopup.update((current) => !current);
	}
</script>

<main>
	<Button text="Toggle Popup" onClick={togglePopup} />
</main> -->
